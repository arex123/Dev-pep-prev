// const fs = require("fs");

// var info = fs.readFileSync("a.txt","utf-8")

// console.log(info);

// var isapresent = fs.existsSync("a.txt")
// var isbpresent = fs.existsSync("b.txt")

// console.log(isapresent);
// console.log(isbpresent);

// fs.writeFileSync("a.txt","how are you"); // it will overwrite the existing file
// fs.writeFileSync("b.txt","hello welcome to file b"); // if file is not present it will make a new file then write something in it

console.log(process.args);