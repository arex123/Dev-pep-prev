let fs = require('fs');
let path = require('path');

let inputArr=process.argv.slice(2); //for input i guess
let mainDir=inputArr[0];
let subDirectory=inputArr.slice(1); //javascript react mongodb

// console.log(mainDir);;
// console.log(subDirectory);
// to run :- node activity2.js webD javascript react mongodb

let mainDirPath=path.join(process.cwd(),mainDir);
for(let i=0;i<subDirectories.length;i++)
{
    console.log();
}
